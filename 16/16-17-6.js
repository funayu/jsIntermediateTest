const obj1 = {
  name: "John Smith",
  age: 28,
  job: "Time-traveller"
};

// Object.valuesはオブジェクトの値を列挙して配列にする
console.log(Object.values(obj1));

// Object.entriesはキーと値の組み合わせで配列にして返す
console.log(Object.entries(obj1));
